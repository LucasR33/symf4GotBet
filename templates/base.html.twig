<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link href="https://stackpath.bootstrapcdn.com/bootswatch/4.2.1/flatly/bootstrap.min.css" rel="stylesheet">
        <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
        <link href="{{ asset('css/css.css') }}" rel="stylesheet" />
        <link href="{{ asset('css/style.css') }}" rel="stylesheet" />
        <style type="text/css">
        @font-face {
         font-family: 'viking';
         src:    url({{asset('/css/font/Game-of-Thrones.otf')}}) format('opentype');;
    }
</style>

    </head>
    <body>
     {% if app.user %}
      <a class="email" href="{{path('compte')}}"> {{app.user.mail}}</a>
     {% endif %}
      <div class="nav-trigger js_navbar">
			<span></span><span></span><span></span>
		  </div>
  		<div class="nav-menu">
  			<ul>
          {% if app.user %}
    				<li><a href="{{path('got_bet')}}"><h2 class="mb">Accueil</h2></a></li>
            <li><a href="{{path('questionnaire')}}"><h2 class="mb">Questions</h2></a></li>
    				<li><a href="{{path('scores')}}"><h2 class="mb">Scores</h2></a></li>
    				<li><a href="{{path('compte')}}"><h2 class="mb">Mon compte</h2></a></li>
          {% endif %}
            <li><a href="{{path('about')}}"><h2 class="mb">A propos</h2></a></li>
          {% if app.user %}
            <li><a href="{{path('app_logout')}}"><h2 class="mb">Déconnexion</h2></a></li>
          {% endif %}
  			</ul>
  		</div>
<section>
  <div class="content">

    </div>
  </section>
<section class="bgwhite">
  <div class="content">

    </div>
  </section>
        {%  block body%} {%endblock %}
    </body>
    <script src="http://code.jquery.com/jquery-3.3.1.js"></script>
    {% block script %}
    {% endblock %}

</html>
<script src="{{ asset('js/nav.js') }}"></script>
<script src='http://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js'></script>
